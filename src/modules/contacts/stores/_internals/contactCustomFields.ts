import { ref } from 'vue';

// dynamic custom-field definitions for the contact entity, read by
// buildContactValidationSchema.ts, set by opened-contact-tabs.vue
export const contactCustomFields = ref([]);
